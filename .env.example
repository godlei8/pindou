# docker compose 用。复制为 .env 并填入真实值。
POSTGRES_PASSWORD=CHANGE_ME
# 生成方式：python -c "import secrets;print(secrets.token_urlsafe(32))"
SESSION_SECRET=CHANGE_ME
# 可选：配了 providers.yaml 才需要
ARK_API_KEY=
DASHSCOPE_API_KEY=
